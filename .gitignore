# Raw / downloaded data (regenerated by scripts; keep repo light)
data/raw/
data/*.rds
*.zip

# R session / history
.Rhistory
.RData
.Rproj.user/
.Renviron

# macOS
.DS_Store

# Build artifacts we don't want to track (we DO commit the final PDF)
*.aux
*.log
